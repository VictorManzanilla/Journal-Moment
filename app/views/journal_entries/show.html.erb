<p style="color: green"><%= notice %></p>

<h2>Your Journal Entry</h2>
<p><%= @journal_entry.content %></p>

<div class="mt-4">
  <%= link_to "Continue Writing (no AI)", edit_journal_entry_path(@journal_entry) %>
  <%= link_to "Ask AI", journal_entry_path(@journal_entry, with_ai: true), method: :get %>
</div>

<% if @conversation_ai %>
  <div class="conversation mt-4 p-2 border rounded">
    <strong>AI Response:</strong>
    <p><%= @conversation_ai.content %></p>
  </div>
<% end %>



<div>
  <%= link_to "Back to journal entries", journal_entries_path %>

  <%= button_to "Destroy this journal entry", @journal_entry, method: :delete %>
</div>
